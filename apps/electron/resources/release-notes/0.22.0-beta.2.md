# 0.22.0-beta.2

## Bug Fixes

- Fixed remote/Tailscale access to the desktop WebUI showing "Login page not found". The 0.22.0-beta.1 build shipped without the WebUI login page bundled inside the app, so remote sign-in never worked. The release build now bundles and verifies the WebUI assets before publishing.
