<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- https://github.com/electron/electron-notarize#prerequisites -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- https://github.com/electron-userland/electron-builder/issues/3940 -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- EventKit access for calendar-backed sources. NOTE: this is an App Sandbox
         entitlement and is INERT today — this app is not sandboxed (no
         com.apple.security.app-sandbox above), and for a non-sandboxed hardened
         runtime app EventKit is gated purely by TCC via the
         NSCalendars*UsageDescription keys in electron-builder.yml extendInfo.
         Kept as the documented pairing so the grant survives if the bundle is
         ever sandboxed; verified harmless (notarization passes with it). It is
         NOT what makes calendar access work — the Info.plist keys are. -->
    <key>com.apple.security.personal-information.calendars</key>
    <true/>
  </dict>
</plist>
